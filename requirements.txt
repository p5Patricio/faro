# Pinned to the exact versions the models and the test suite were built
# against. A trained model's `.joblib` can fail to load if numpy /
# scikit-learn / joblib change between training and inference, so these are
# `==`, not `>=`. Bump deliberately, then retrain and re-run the suite.

# --- runtime ---
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
yfinance==1.5.1
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
joblib==1.5.3
python-dotenv==1.2.2
requests==2.33.1
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
PyPortfolioOpt==1.6.0

# FinBERT sentiment scoring (brain/sentiment_factors.py). Heavy and OPTIONAL
# for the test suite: transformers/torch are imported lazily, inside
# brain.sentiment_factors._default_finbert_scorer, never at module import
# time, so a plain `pytest` run never pays this cost and never needs these
# two packages importable at all -- only genuine production scoring
# (brain/materialize_sentiment.py, ops/materialize_sentiment_batch.py) does.
transformers==4.57.1
torch==2.9.0

# --- test / dev ---
pytest==9.0.3
httpx==0.28.1
